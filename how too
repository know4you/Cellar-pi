to download start with a fresh pie install im testing in pie lite 32 bit
you need to sign in via ssh 
  copy and paste
hostname
whoami
cat /etc/os-release | grep PRETTY_NAME
  make sure you see your pie name
then we need to update the pie using this 
  
sudo apt update
sudo apt install -y git
should start downloading and applying updates when its done we need to copy and paste 
cd ~
git clone https://github.com/know4you/Cellar-pi.git
cd Cellar-pi
    make sure the files are there by
ls -la
  You should see something like:
README.md
cellar_logger.py
install.sh
requirements.txt
setup.sh
...

Before we actually run the installer
Let's do a quick sanity check on the scripts:
 bash -n install.sh
bash -n setup.sh
python3 -m py_compile cellar_logger.py

  If those commands produce no output, that's a good sign—they passed their syntax checks.
now lets intall :) 
sudo bash install.sh
